[project]
name = "buildgpt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Lei Ni", email = "nileichina@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "torch>=2.14.0",
    "triton>=3.8.0",
]

[project.scripts]
buildgpt = "buildgpt:main"

[[tool.uv.index]]
url = "https://download.pytorch.org/whl/xpu"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
